import { graphql, useStaticQuery } from 'gatsby';
import React from 'react';

export const BlogRoll: React.FC = ({  }) => {

    
    return (
        <div>
            hi
            {/* {posts.map((el: any) => <div>
                <span>{el.frontmatter.title}</span>
            </div>)} */}
        </div>
    )
}